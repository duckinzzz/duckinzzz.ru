x-app-image: &app-image ghcr.io/duckinzzz/duckinzzz.ru:latest

networks:
  duckinzzz_net:
    name: duckinzzz_net
    driver: bridge
  proxy:
    external: true

services:
  web:
    image: *app-image
    container_name: duckinzzz_web
    mem_limit: 180m
    cpus: 0.3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "2"
    env_file:
      - .env
    volumes:
      - static_volume:/app/static
      - media_volume:/app/media
    expose:
      - "8000"
    entrypoint: [ "/bin/bash", "/app/entrypoint.sh" ]
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - duckinzzz_net

  celery:
    image: *app-image
    mem_limit: 120m
    cpus: 0.2
    env_file:
      - .env
    depends_on:
      - redis
      - web
    networks:
      - duckinzzz_net
    entrypoint: [ ]
    command: celery -A config worker --loglevel=warning --concurrency=1 --max-tasks-per-child=50
    restart: unless-stopped

  celery-beat:
    image: *app-image
    mem_limit: 80m
    cpus: 0.1
    command: celery -A config beat --loglevel=warning
    env_file:
      - .env
    depends_on:
      - redis
      - web
    networks:
      - duckinzzz_net
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: duckinzzz_redis
    ports:
      - "6379:6379"
    networks:
      - duckinzzz_net
    restart: unless-stopped
    command: redis-server --maxmemory 50mb --maxmemory-policy allkeys-lru --save "" --appendonly no

  nginx:
    image: nginx:alpine
    container_name: duckinzzz_nginx
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "2"
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - static_volume:/app/static:ro
      - media_volume:/app/media:ro
    depends_on:
      - web
    restart: unless-stopped
    networks:
      - duckinzzz_net
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.site.rule=Host(`duckinzzz.ru`) || Host(`www.duckinzzz.ru`)"
      - "traefik.http.routers.site.entrypoints=websecure"
      - "traefik.http.routers.site.tls=true"
      - "traefik.http.routers.site.priority=100"
      - "traefik.http.routers.site.middlewares=cow-errors@file"
      - "traefik.http.services.site.loadbalancer.server.port=80"

volumes:
  static_volume:
  media_volume: